.container
  %center 
    %h2== "#{@category.title}" category info: 
    %h4
      %br
      == Category title: <strong>#{@category.title}</strong>
      %br== We have <strong>#{@category.number_of_books}</strong> books of this category
    %center
      %br
      - if customer_signed_in? && current_customer.admin == true
        =link_to 'Edit category info', edit_category_path, class: 'btn btn-primary'
        =link_to 'Delete category', category_path, method: :delete, class: 'btn btn-danger'