%h2== Editing "#{@category.title}" category info
= form_for @category do |cat|
  = render partial: "errors/errors", locals: {object: cat.object}
  = cat.text_field :title, placeholder:"Type category title here.."
  %br
  %br= cat.submit
